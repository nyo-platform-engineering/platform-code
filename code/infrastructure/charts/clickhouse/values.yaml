image: altinity/clickhouse-server:26.3.33.10001.altinitystable
storageClassName: local-path
storage:
  clickhouse: 5Gi
credentials:
  appPassword: local-clickhouse-app
  ingestPassword: local-clickhouse-otel
  schemaPassword: local-clickhouse-schema
schema:
  enabled: true
  sql: |
    CREATE DATABASE IF NOT EXISTS custom;
    -- Add idempotent CREATE TABLE / CREATE VIEW statements here.
