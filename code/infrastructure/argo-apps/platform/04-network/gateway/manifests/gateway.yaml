apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  labels:
    platform.local/owner: platform-team
  name: local
  namespace: traefik
spec:
  gatewayClassName: traefik
  listeners:
    - name: web
      port: 8000
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: All
