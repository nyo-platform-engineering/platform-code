credentials:
  # Local-development credentials. Replace them outside a disposable cluster.
  appPassword: local-clickhouse-app
  ingestPassword: local-clickhouse-otel
  schemaPassword: local-clickhouse-schema
schema:
  sql: |
    CREATE DATABASE IF NOT EXISTS custom;
    -- Keep migrations idempotent. Add custom tables and views below.
